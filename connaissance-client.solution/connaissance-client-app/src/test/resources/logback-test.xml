<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- Console Appender with MDC for tests -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [userId=%X{userId:-anonymous}] [correlationId=%X{correlationId:-}] [clientId=%X{clientId:-}] [operation=%X{operation:-}] - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Root logger for tests -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
    
    <!-- Application loggers -->
    <logger name="com.sqli.workshop.ddd" level="DEBUG"/>
    <logger name="io.github.resilience4j" level="DEBUG"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.data.mongodb" level="INFO"/>
</configuration>
