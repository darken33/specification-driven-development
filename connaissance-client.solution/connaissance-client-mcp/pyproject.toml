[project]
name = "connaissance-client-mcp"
version = "0.1.0"
description = "MCP Server for Connaissance Client API"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.2.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]

[project.scripts]
connaissance-client-mcp = "server:main"
